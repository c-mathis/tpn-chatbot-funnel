{
  "name": "tpn-funnel",
  "version": "1.0.0",
  "description": "Tax Peace Now funnel site",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve",
    "dev": "npx @11ty/eleventy --serve --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
